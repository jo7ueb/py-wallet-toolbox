[project]
name = "bsv-wallet-toolbox"
version = "0.6.0"
description = "BRC-100 compliant Bitcoin SV wallet implementation"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Open BSV License version 4"}
authors = [
    {name = "BSV Blockchain Association"},
]
keywords = ["bitcoin", "bsv", "wallet", "brc-100"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "bsv-sdk @ git+https://github.com/bsv-blockchain/py-sdk.git@develop-port",
    "SQLAlchemy>=2.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.21.0",
    "mypy>=1.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]
db-mysql = [
    "pymysql>=1.1",
]
db-postgres = [
    "psycopg[binary]>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.black]
line-length = 120
target-version = ['py311']
skip-string-normalization = false

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "ANN", "B", "A", "C4", "PIE", "T20", "SIM", "ARG", "PTH", "ERA", "PL", "RUF"]
# Ignore common test-related warnings
ignore = [
    "ERA001",   # commented-out-code (acceptable in tests)
    "PLR2004",  # magic-value-comparison (acceptable in tests)
    "ARG005",   # unused-lambda-argument (fixtures)
    "ANN401",   # any-type (acceptable for flexible types)
    "A006",     # builtin-lambda-argument-shadowing (fixtures)
    "ARG001",   # unused-function-argument (test fixtures)
    "PLR0915",  # too-many-statements (acceptable in tests)
    "PLR0912",  # too-many-branches (acceptable in tests)
    "PLR0911",  # too-many-return-statements (complex but correct logic)
    "B017",     # assert-raises-exception (pytest pattern)
    "T201",     # print (acceptable in manual tests)
    "ANN204",   # missing-return-type-special-method
    "E722",     # bare-except (some legacy code)
    "SIM105",   # suppressible-exception
    "A002",     # builtin-argument-shadowing
    "B023",     # function-uses-loop-variable
    "SIM102",   # collapsible-if
    "SIM108",   # if-else-block-instead-of-if-exp
    "A001",     # builtin-variable-shadowing
    "RUF013",   # implicit-optional
    "B904",     # raise-without-from-inside-except
    "PLR1714",  # repeated-equality-comparison
]

[tool.ruff.lint.per-file-ignores]
# Test files have different standards
"tests/**/*.py" = ["ANN001", "ANN002", "ANN003", "ANN201", "ANN202", "E501", "B018"]
"manual_tests/**/*.py" = ["ANN001", "ANN002", "ANN003", "ANN201", "ANN202", "E501", "PLC0415"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --strict-markers"
markers = [
    "manual: marks tests as manual tests (deselect with '-m \"not manual\"')",
    "asyncio: marks tests as async tests",
]
# Entity tests deferred to Phase 5 (see conftest_entity_skip.py)

[tool.coverage.run]
source = ["src"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false

